-- This script creates a table called first_table in the current database
-- Command to create a table with specific columns if it does not exist
CREATE TABLE IF NOT EXISTS first_table (
    id INT ,
    name VARCHAR(256)
);
